<div class="floating-label">
    <input name="{{ $name }}" id="{{ $name }}" value="{{ $value }}" class="floating-input" placeholder=" " type="{{ $type }}" data-mask="+38(000)-000-00-00" />
    <label class="required">{{ $label }}</label>
</div>